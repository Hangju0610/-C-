#include <bits/stdc++.h>
using namespace std;
int main() {
    char a = 'a';
    cout << (int)a << '\n';
    cout << (int)a - 97 << '\n';
    cout << (int)a - 'a' << '\n';
    return 0;
}

/*
A ~ Z 는 65 ~ 90
a ~ z 는 97 ~ 122의 아스키 코드를 가지고 있다.
a ~ z까지 입력을 받는데 이를 정수 0 ~ 25까지 표현하고 싶다면 다음과 같이 하면 된다.
*/