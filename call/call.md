## 참조에 의한 호출로 넘겨야 할 때
원시적 타입, double, int 등은 "값에 의한 호출"로 넘기는 게 좋다.
복사가 일어나지만 간단하기 때문에 복사에 대한 코스트가 크지 않다.

하지만 reference한 타입 중 복잡한 struct나 많은 요소를 가진 배열은
배열이 차지하는 메모리가 많을 떄는 참조로써 매개변수를 넘기는 것이 좋다.

참조 없이 전달하게 될 경우 전체 복사를 해야 하는데 이 때 드는 CPU, 메모리 등 코스트가 더 크기 때문이다.