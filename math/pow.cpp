#include <bits/stdc++.h>
using namespace std;

int main() {
    int n = 4;
    int pow_2 = (int)pow(2, n);
    cout << pow_2 << '\n';
    return 0;
}

/*
pow(double base, double exponent);

기본적으로 double을 반환하기 때문에 int 형을 사용하고 싶다면 int로 형변환을 한다.
*/